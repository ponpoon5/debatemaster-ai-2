# Gemini API Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Proxy Mode (for local development)
# Set to true to use local proxy server, false to call Gemini API directly
VITE_USE_PROXY=false

# Proxy URL (only needed if VITE_USE_PROXY=true)
# For local development: http://localhost:3000
# For production with separate proxy server: https://your-proxy-server.com
VITE_PROXY_URL=

# Gemini API Key (for client-side direct calls)
# ⚠️ WARNING: This will be exposed in browser. Use proxy mode for production.
# Only set this if VITE_USE_PROXY=false
VITE_GEMINI_API_KEY=
